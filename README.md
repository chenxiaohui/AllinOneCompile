AllinOneCompile
===============

A vim plugin to config the compile options of all kinds of file

##markdown test

	:::python
	class Foo:
	  def hello(self):
		print("hello, world")

	f = Foo()
	f.hello()

